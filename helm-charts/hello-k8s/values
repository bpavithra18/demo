replicaCount: 1

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  ingressClassName: nginx
  host: demo.example.com    # replace with your domain
  tls:
    enabled: true
    secretName: demo-tls

resources: {}
